message=Hello from dev1
# Application name must match the config file name in Git repo
spring.application.name=app1

# Config Server URL
spring.cloud.config.uri=http://localhost:8888

# Fail fast if Config Server is not reachable
spring.cloud.config.fail-fast=true

# Optional: profile and label (branch in Git)
spring.cloud.config.profile=dev
spring.cloud.config.label=main

